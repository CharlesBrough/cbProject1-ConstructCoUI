const showAdmin = () => {
  if (isAdmin) {
    return (
      //stuff
      null
    );
  }
};

const checkAdmin = () => {
  //Get datas and if admin then set state isAdmin = true
};

const login = () => {
  //Fetch/Axios to server
  //Return the cookie?
};
